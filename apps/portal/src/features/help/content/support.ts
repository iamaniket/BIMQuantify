/**
 * Support contact address surfaced in the Help center (the "Support" KPI tile).
 *
 * TODO(confirm): replace with the real support inbox before launch. Defaults to the
 * `bimdossier.dev` mail domain used by the API (`apps/api/.env.example` → `SMTP_FROM`).
 */
export const SUPPORT_EMAIL = 'support@bimdossier.dev';
