/**
 * NL jurisdiction module — Dutch address lookup (PDOK) + aerial map
 * thumbnails (PDOK WMS). New jurisdictions ship as sibling folders;
 * see ../README.md if added.
 */

export * from './addressLookup';
export * from './mapThumbnail';
