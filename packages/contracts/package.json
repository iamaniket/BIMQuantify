{
  "name": "@bimdossier/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "Cross-package/service magic-value contracts (binary format tags, bridge globals, id formats). Single source of truth so the two sides of a seam can't silently drift.",
  "main": "./index.cjs",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "default": "./index.cjs"
    }
  },
  "files": [
    "index.cjs",
    "index.d.ts"
  ]
}
